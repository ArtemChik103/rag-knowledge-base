# Алгоритмы RAG-системы

В данном документе формализованы математические и логические алгоритмы, используемые в модулях индексации, векторного поиска и генерации ответов.

---

## 1. Алгоритм рекурсивного разбиения текста (Recursive Text Splitting)

### Входные данные
- Исходный текст документа $T$ длины $N$ символов.
- Целевой размер чанка $S = 500$ символов.
- Размер перекрытия $O = 100$ символов ($O < S$).
- Иерархический список разделителей $D = [\text{"\textbackslash n\textbackslash n"}, \text{"\textbackslash n"}, \text{". "}, \text{"! "}, \text{"? "}, \text{"; "}, \text{", "}, \text{" "}]$.

### Шаги алгоритма
1. **Рекурсивное разделение по иерархии:**
   - Если длина текущего фрагмента $\le S$, фрагмент сохраняется.
   - Иначе выбирается первый разделитель $d \in D$, присутствующий во фрагменте.
   - Фрагмент делится по разделителю $d$.
   - Если полученные части превышают $S$, к ним рекурсивно применяется следующий разделитель из $D$.
2. **Формирование блоков с перекрытием:**
   - Элементы объединяются последовательно в буфер $B$ до тех пор, пока $\text{len}(B) + \text{len}(\text{part}) \le S$.
   - При превышении размера $S$ текущий буфер $B$ фиксируется как готовый чанк $C_i$.
   - Новый буфер инициализируется суффиксом предыдущего буфера длины $O$, обрезанным по границе слова, после чего к нему добавляется текущая часть:
     $$B_{i+1} = \text{Suffix}(B_i, O) + \text{part}$$
3. **Генерация метаданных:**
   - Для каждого чанка $C_i$ вычисляются:
     - `chunk_id`: уникальный ключ вида `{doc_id}_{page}_{index}`.
     - `page_number`: номер исходной страницы PDF.
     - `start_char`, `end_char`: абсолютные границы текста на странице.

---

## 2. Алгоритм вычисления эмбеддингов

### Входные данные
- Набор текстовых фрагментов $\{C_1, C_2, \dots, C_m\}$.
- Нейросетевая модель Transformer размера $L=12$ слоев, $H=384$ скрытая размерность.

### Шаги алгоритма
1. **Токенизация:**
   - Текст $C_i$ токенизируется с помощью WordPiece/SentencePiece с ограничением до 512 токенов.
2. **Прямой проход (Forward Pass):**
   - Вычисляются скрытые состояния последнего слоя:
     $$H_i = \text{Transformer}(\text{Tokens}(C_i)) \in \mathbb{R}^{T \times 384}$$
3. **Усредняющее пулирование (Mean Pooling):**
   - Вектор предложения формируется усреднением токенов с учетом маски внимания:
     $$\mathbf{e}_i = \frac{\sum_{t=1}^T M_{i,t} \cdot H_{i,t}}{\sum_{t=1}^T M_{i,t}}$$
4. **$L_2$-Нормализация:**
   - Вектор нормализуется на единичную сферу:
     $$\mathbf{v}_i = \frac{\mathbf{e}_i}{\|\mathbf{e}_i\|_2} = \frac{\mathbf{e}_i}{\sqrt{\sum_{j=1}^{384} e_{i,j}^2}}$$

---

## 3. Алгоритм векторного поиска (HNSW Cosine Retrieval)

### Входные данные
- Вектор пользовательского запроса $\mathbf{q} \in \mathbb{R}^{384}$, $\|\mathbf{q}\|_2 = 1$.
- Векторная база данных с HNSW-графом (Hierarchical Navigable Small World).
- Параметр $K = 4$ (количество ближайших соседей).

### Шаги алгоритма
1. **Поиск в HNSW-графе:**
   - Поиск начинается с верхнего слоя графа с крупным шагом перехода между узлами.
   - На каждом слое выполняется жадный спуск к локальному минимуму расстояния.
   - На базовом слое (Layer 0) формируется список из $K$ ближайших вершин.
2. **Расчет косинусного расстояния:**
   - Для нормализованных векторов косинусное расстояние выражается через скалярное произведение:
     $$d(\mathbf{q}, \mathbf{v}_i) = 1 - \langle \mathbf{q}, \mathbf{v}_i \rangle = 1 - \sum_{j=1}^{384} q_j \cdot v_{i,j}$$
3. **Преобразование в оценку сходства (Similarity Score):**
   - Значение нормализуется в диапазон $[0.0, 1.0]$:
     $$\text{Score}(\mathbf{q}, \mathbf{v}_i) = \max\left(0.0, 1.0 - \frac{d(\mathbf{q}, \mathbf{v}_i)}{2.0}\right)$$
4. **Пороговая фильтрация:**
   - Отбираются только чанки, удовлетворяющие условию:
     $$\text{Score}(\mathbf{q}, \mathbf{v}_i) \ge \theta_{\text{threshold}} \quad (\theta = 0.20)$$

---

## 4. Алгоритм извлечения ответа и формирования цитат

### Входные данные
- Текст запроса $Q$.
- Список отфильтрованных чанков $\{C_1, C_2, \dots, C_k\}$.

### Шаги алгоритма
1. **Извлечение ключевых терминов:**
   - Текст запроса очищается от знаков препинания и стоп-слов.
   - Формируется список значимых лексем $W_Q$.
2. **Сегментация и ранжирование предложений:**
   - Каждый чанк $C_i$ разбивается на предложения $\{s_{i,1}, s_{i,2}, \dots\}$.
   - Для каждого предложения рассчитывается вес соответствия:
     $$\text{Weight}(s_{i,j}) = 2.0 \cdot \sum_{w \in W_Q} \mathbb{I}(w \in s_{i,j}) + \text{Score}(C_i)$$
3. **Синтез ответа:**
   - Предложения сортируются по убыванию веса $\text{Weight}$.
   - Выбираются топ-3 уникальных фактологических предложения.
   - Формируется ответ с указанием номеров страниц и регламентов.
4. **Сборка цитат (Citations):**
   - Для каждого релевантного чанка генерируется объект цитаты:
     ```json
     {
       "chunk_id": "doc1_page1_0",
       "filename": "sample_company_policy.pdf",
       "page_number": 1,
       "chunk_index": 0,
       "score": 0.8421,
       "snippet": "1.2. Базовый рабочий график: начало рабочего дня в 09:00, окончание в 18:00..."
     }
     ```
