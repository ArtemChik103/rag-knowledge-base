# Алгоритмы и математический аппарат RAG-системы

## 1. Алгоритм парсинга и сегментации документов

### 1.1. Блочное извлечение из PDF (C-Engine MuPDF)
Пусть документ PDF состоит из страниц \( P = \{p_1, p_2, \dots, p_N\} \).
Для каждой страницы \( p_i \) скомпилированный движок MuPDF возвращает упорядоченный набор прямоугольных текстовых блоков:
\[
B(p_i) = \{ (x_0, y_0, x_1, y_1, T_k, k) \mid k \in [1, K_i] \}
\]
где \( T_k \) — текст смыслового блока, а координаты определяют топологический порядок чтения (Top-to-Bottom, Left-to-Right).

Внутри каждого блока \( T_k \) выполняется нормализация визуальных переносов строк:
\[
\tilde{T}_k = \text{join}\left(\{ l_j \in \text{splitlines}(T_k) \mid l_j \neq \emptyset \}, \text{ " "}\right)
\]

### 1.2. Рекурсивное чанкирование с контекстным перекрытием
Текст страницы разбивается с использованием иерархических разделителей \( S = [\text{"\n\n"}, \text{"\n"}, \text{". "}, \text{" "}] \) при параметрах:
- Размер чанка: \( C_{\max} = 750 \) символов.
- Перекрытие чанков: \( O = 120 \) символов.

При перекрытии гарантируется сохранение границ слов и цельности регламентных пунктов:
\[
\text{Chunk}_m = [t_{\text{start}}, t_{\text{end}}], \quad \text{len}(\text{Chunk}_m) \le C_{\max}
\]
\[
t_{\text{start}}^{(m+1)} = \max(t_{\text{start}}^{(m)}, t_{\text{end}}^{(m)} - O)
\]

---

## 2. Алгоритм векторного представления (ONNX Runtime & PyTorch SIMD)

### 2.1. Прямой проход трансформера
Текст чанка \( T \) преобразуется токенизатором в последовательность целочисленных идентификаторов токенов \( X = (x_1, x_2, \dots, x_L) \).

Модель трансформера вычисляет скрытые состояния последнего слоя:
\[
H = \text{Transformer}(X) \in \mathbb{R}^{L \times d}, \quad d = 384
\]

### 2.2. Усредняющий пулинг с маской внимания (Mean Pooling)
Для учета переменной длины последовательностей и исключения токенов заполнения (padding) применяется маскированный пулинг:
\[
\mathbf{v}_{\text{raw}} = \frac{\sum_{j=1}^{L} H_j \cdot M_j}{\max\left(10^{-9}, \sum_{j=1}^{L} M_j\right)}, \quad M_j \in \{0, 1\}
\]

### 2.3. \(L_2\)-нормализация
Для сопоставления векторов по косинусному расстоянию выполняется нормировка вектора в единичную гиперсферу:
\[
\mathbf{e} = \frac{\mathbf{v}_{\text{raw}}}{\|\mathbf{v}_{\text{raw}}\|_2} = \frac{\mathbf{v}_{\text{raw}}}{\sqrt{\sum_{i=1}^{d} v_i^2}}
\]

### 2.4. LRU-кэширование (O(1) доступ)
Для запроса \( q \) вычисляется ключ кэша \( k = \text{hash}(q) \).
\[
\mathbf{e}(q) = \begin{cases} 
\text{Cache}[k], & \text{если } k \in \text{Cache} \quad (\Delta t \approx 0.001\text{ мс}) \\
\text{ONNX\_Engine}(q), & \text{иначе (с последующим сохранением в Cache)}
\end{cases}
\]

---

## 3. Алгоритм векторного поиска (HNSW Cosine Space)

Косинусное сходство между вектором запроса \( \mathbf{e}_q \) и вектором чанка \( \mathbf{e}_c \):
\[
\text{Similarity}(\mathbf{e}_q, \mathbf{e}_c) = \mathbf{e}_q \cdot \mathbf{e}_c = \sum_{i=1}^{d} e_{q, i} \cdot e_{c, i} \in [0.0, 1.0]
\]
Косинусное расстояние в ChromaDB:
\[
D_{\text{cosine}}(\mathbf{e}_q, \mathbf{e}_c) = 1.0 - \text{Similarity}(\mathbf{e}_q, \mathbf{e}_c)
\]

HNSW (Hierarchical Navigable Small World) выполняет приближенный поиск \(k\) ближайших соседей с логарифмической сложностью \( \mathcal{O}(\log N) \).

---

## 4. Алгоритм фактологического локального синтеза ответа

Для каждого извлеченного чанка \( c \in \text{TopK} \) выделяются смысловые блоки и вычисляется фактологический вес:
\[
\text{Score}(B_j) = w_1 \cdot \text{TermMatches}(B_j, Q) + w_2 \cdot \mathbb{I}_{\text{FactNum}}(B_j) + w_3 \cdot \text{Similarity}(c)
\]
где:
- \( w_1 = 3.5 \) (вес лексического совпадения ключевых терминов),
- \( w_2 = 3.0 \) (фактологический бонус за наличие временных интервалов `HH:MM`, денежных сумм, дней и процентов),
- \( w_3 = 2.0 \) (вес косинусного сходства векторного слоя).

Отобранные блоки объединяются в структурированный ответ с указанием точных страниц документов.
